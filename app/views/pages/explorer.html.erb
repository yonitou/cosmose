
<div class="explorer-top d-flex flex-column">
  <div class="explorer-choice d-flex justify-content-between align-items-center">
    <%= link_to "PROJETS", explorer_path, class:"btn btn-primary btn-choice-explorer" %>

    <%= link_to "ARTISTES", explorer_path, class:"title-choice-explorer" %>
  </div>
  <div class="explorer-search">
    <input type="text" placeholder="Categories">
    <input type="text" placeholder="Statut">
    <input type="text" placeholder="Je cherche à ...">
    <a href="#" class="cta">
      <span class="search-effect-explorer">Rechercher</span>
      <svg class="svg-explorer" width="13px" height="10px" viewBox="0 0 13 10">
        <path d="M1,5 L11,5"></path>
        <polyline points="8 1 12 5 8 9"></polyline>
      </svg>
    </a>
  </div>
</div>

<div class="explorer-content">
  <%= render 'shared/card-project' %>
  <% @projects.each do |project| %>
    <%= project.title %>
    <%= project.description %>
  <% end %>

  <% @users.each do |user| %>
   <%= user.username %>
  <% end%>

</div>
